apiVersion: v1
kind: Service
metadata:
  name: pgadmin
  labels:
    app: pgadmin
    app.kubernetes.io/name: pgadmin
    app.kubernetes.io/component: admin-ui
spec:
  type: ClusterIP
  selector:
    app: pgadmin
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
