apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Namespace
  - namespace-ai.yaml

  # cert-manager CA configuration (ClusterIssuers)
  - cert-manager/clusterissuer.yaml

  # AI Stack components
  - apps/ai-stack

  # Ingress routes (with TLS via cert-manager)
  - ingress/openwebui-ingress.yaml
  - ingress/control-ingress.yaml
