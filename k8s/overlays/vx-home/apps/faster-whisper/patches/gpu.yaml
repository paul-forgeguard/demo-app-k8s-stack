apiVersion: apps/v1
kind: Deployment
metadata:
  name: faster-whisper
spec:
  template:
    spec:
      nodeSelector:
        gpu: "small"
      containers:
      - name: faster-whisper
        resources:
          limits:
            nvidia.com/gpu: 1
